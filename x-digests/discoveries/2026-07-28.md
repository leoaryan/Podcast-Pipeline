# Discoveries — 2026-07-28

## Investigate
- **Gemini CLI (google-gemini/gemini-cli)**: Open-source AI agent turning terminal into coding/operational agent powered by Gemini. Reads/edits codebases, supports MCP for external tools, hierarchical GEMINI.md for context, headless modes, grounded search. Apache 2.0. Strong design for daily terminal AI workflows.  
  Why: Real agentic CLI with production-relevant features (MCP, project conventions). GitHub: https://github.com/google-gemini/gemini-cli  
  Next: Clone repo, explore MCP integration and GEMINI.md patterns.

- **PentesterFlow**: Human-in-the-loop agentic AI CLI for pentesters/bug bounty. Automates recon-to-reporting with explicit approval gates. Supports Ollama/Groq/Gemini/OpenAI, built-in pentest skills (recon, vulns, SSRF etc.), real tool execution (shell/HTTP/Burp/browser), evidence reports, local learning, strong safety (redaction, blocks dangerous cmds).  
  Why: Practical agent design with safety harnesses — directly addresses production reliability concerns.  
  Next: Search for GitHub repo or detailed docs; test permission/safety model if available.

- **Prompt Caching + KV Cache Patterns (Anthropic/OpenAI/etc.)**: Widespread adoption (~80% input tokens cached by July 2026). Rule: static content (system/tools/examples/docs) first, dynamic last. 70-90% savings on cached tokens + TTFT wins. Providers support explicit controls (cache_control). Semantic caching emerging.  
  Why: Highest-ROI engineering pattern for cost/latency in real LLM apps. Directly actionable.  
  Next: Review provider docs (Anthropic cache_control, OpenAI prefix caching), prototype a static-first prompt template.

- **Agent Patterns Taxonomy (Anthropic-inspired)**: 6 core patterns — Prompt Chaining, Routing, Parallelization, Orchestrator-Workers, Evaluator-Optimizer, Autonomous Agent. Rule: start with 1-5 (workflows); add full autonomy only when needed. Multi-agent extensions: specialized teams, guardrails, swarms with cheap models + shared artifacts.  
  Why: Concrete, production-tested taxonomy with clear tradeoffs and starting advice. Bridges theory to implementation.  
  Next: Map one current workflow to these patterns; prototype Orchestrator-Workers or Evaluator-Optimizer.

- **pvcli (Cloudflare)**: New curl-like CLI debugger for privacy-preserving protocols (OHTTP etc.). Built from production systems handling millions of req/s. Just open-sourced.  
  Why: High-signal release from credible source on niche but important protocols.  
  Next: Find release notes/GitHub; experiment with OHTTP flows if relevant.

## Interesting
- **Unity CLI**: Official standalone CLI with `unity command eval` for live C# expressions in running Editor (no domain reload/recompile). Strong for automation/agents/testing.
- **Agent Harness / "OS for agents"**: Focus on surrounding engineering (human approvals, interactive steering, crash recovery, multi-provider abstraction, observability like Opik) over raw tool-calling.
- **Pseudo-code + explicit module boundaries**: Effective incremental coding pattern for hard problems (declare interfaces, reference docs, build thin wrappers).
- **Model routing + cache awareness**: Dynamic model choice with gotchas around invalidating accumulated caches; assign roles to fixed cached models.
- **Swarm architectures**: Lightweight cheap-model agents coordinated by frontier model for planning; shared design docs and conflict resolution.
- **claude-cmd**: Lightweight CLI for managing Claude Code configs, slash commands, CLAUDE.md files across projects.
- **Boundary objects**: Semantic diffs, execution traces, tradeoff matrices instead of raw AI output dumps for better human-AI collaboration.

## Quiet Day
No compelling discoveries today. (There are solid technical leads above meeting the 2-hour investigation bar.)