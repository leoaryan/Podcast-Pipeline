# Discoveries — 2026-07-24

## Investigate

**inshellisense** — Terminal tool that brings IDE-style autocomplete (bash/zsh/fish/PowerShell) for 600+ CLI tools with intelligent completions. Practical daily driver for better shell UX.
- Link: osp.fyi/inshellisense (via aggregator post)
- Why interesting: Real engineering focus on completions rather than another wrapper; worth testing against common workflows.
- Suggested next step: Install via the repo and benchmark completion quality/speed on a few tools (e.g., git, docker, kubectl).

**Graph Engineering for scalable multi-agent systems** — Pattern using a central topological graph to generate architecture and route 120+ pathways between 15 autonomous "loop agents" from a single ~200-word prompt. Agents self-correct via continuous state sharing instead of brittle prompt chaining.
- Link: x.com/slash1sol/status/2080048779659796555
- Why interesting: Concrete shift from manual chaining to graph + loop engineering; high signal for production multi-agent design.
- Suggested next step: Read the full thread, map the graph structure, and prototype a minimal version in LangGraph or similar.

**Model affinity + prompt caching hack ("Cache Trap")** — In agentic workflows, routing easy/hard queries to different models breaks cache warmth across growing context. Solution: route once per task based on difficulty, then pin the model for the entire task to preserve ~90% cache discounts.
- Link: x.com/alex_prompter/status/2079862160386302343
- Why interesting: Directly actionable cost optimization tied to real provider pricing (cache_read vs write).
- Suggested next step: Implement a simple task-level router that enforces model pinning and measure cache hit rates/costs on a sample agent loop.

**Perry (Rust TS-to-native compiler)** — Rust-based compiler that turns TypeScript directly into native binaries without runtime or Electron.
- Link: Mentioned in open-source/AI infra discussions (commitvc thread)
- Why interesting: Eliminates JS runtime overhead for TS apps; unusual cross-language compilation approach worth understanding.
- Suggested next step: Locate the GitHub repo, review the compiler architecture, and test compilation of a small TS project.

## Interesting

- **CLI utility for shadcn projects** feeding structured evidence to coding agents (Cursor etc.) instead of vague prompts — improves agent feedback loops.
- **OpenSquilla** microkernel agent runtime using model routing + syscall sandboxing to cut token usage.
- **Semantic caching** pattern with embeddings (e.g., all-mpnet-base-v2 + FAISS/Redis) for prompt/context reuse, plus exact-match fallback.
- **"Start at step 7" (monitoring/maintenance first)** workflow design hack from someone who shipped (and failed) 12 automation bots.
- **Trust ladder + agent-on-call** for safe production agent scaling (auto-remediate low-risk, human approval higher-risk).
- **VLM → physics simulator pipeline** for iterative scene extraction, simulation, and validation (agent-in-the-loop for simulator building).
- **Foam-Agent / ChatCFD** style LLM multi-agent automation for physics solvers (meshing, setup, execution, error correction via natural language).
- **Reducing tool calls aggressively** (e.g., 41→12) and fusing steps to halve LLM costs even with larger context, by understanding cache pricing.
- **LLM-driven simulated worlds** for agents (SIM > Graph > Loops hierarchy) to test/iterate in a live loop.
- **Provider-agnostic LLM abstraction** early in pipelines for easy model swapping.

## Quiet Day

No compelling discoveries today.