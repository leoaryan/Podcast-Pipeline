# Discoveries — 2026-08-04

## Investigate
- **grok-cli (superagent-ai)**: Open-source terminal coding agent for xAI Grok API. Supports interactive sessions (OpenTUI), headless/scripted mode, built-in X/web search, sub-agents, sandboxed verification (build/test/browser with screenshots & video), Telegram voice control. MIT license, rapid star growth. Repo: https://github.com/superagent-ai/grok-cli. Next: Clone, install, run a simple coding task end-to-end to evaluate verification and tool-use quality.
- **countershape long-running autonomous agent (nelsonwerd)**: 15+ day unattended "idea-to-ship" pipeline with strict evidence-based verification via didrun recorder (commands, exit codes, hashes). Repo-enforced rules (file access lists, SHA checkers, prose tests, linter constraints), single-writer serialization, persona that fades but doctrine persists. Key insight: verification cost scales with proof history. Repo: https://github.com/nelsonwerd/countershape (with git notes). Next: Read the debrief and core scripts; experiment with a minimal version of the evidence chaining on a small task.
- **Pi local multi-model harness + DeepSeek cluster (runsonai)**: Hybrid setup running DeepSeek V4 Flash locally (on dual ASUS GX10 hardware) as primary brain with Qwen vision support; frontier models only for high-stakes planning. Includes pi-local-harness for safety/recovery, /goal mode for durable objectives, strict external verification. Solves "one model can't do everything" economically with fast local responses. Harness published. Next: Review the harness code and goal patterns; replicate a simple hybrid routing example.
- **NOOA (NVIDIA)**: Open-source OOP framework treating agents as a single Python class (methods = actions, fields = state, docstrings = prompts, type annotations = structured output contracts). Smart handling for large data (model sees summaries only; full data stays in Python session). SWE-bench Verified gains with fewer calls/tokens. Next: Clone the repo, examine the class structure, and port a small agent example to test the interface and data optimization.
- **Context engineering patterns (AGENTS.md / SKILL.md)**: Standardized repo-root Markdown files for persistent agent context (build commands, constraints, "done" criteria) and progressive-disclosure skills (YAML frontmatter + lazy loading). Treat as living engineering artifacts reviewed in PRs; ruthlessly prune inference-able content. Widely adopted (30+ tools, 60k+ repos). Next: Audit an existing repo or create minimal examples for a test project to measure token savings and reliability impact.

## Interesting
- GitHub CLI "agent skill" extension: Brings PRs/issues/GitHub commands into terminal for coding agents.
- OpenWiki (LangChain): CLI that auto-writes/maintains codebase documentation.
- Codex Security CLI (OpenAI): Terminal tool for finding/fixing vulnerabilities.
- Outcome-maxxing tactics: Model routing by ambiguity/reversibility/verifiability, strict prompt prefix caching for KV hits, hard loop budgets, output caps (diffs/patches only).
- Recursive self-improvement overview: Emphasis on harnesses, synthetic data with challenger/verifier roles, and independent verifiers as the real ceiling.

## Quiet Day
No compelling discoveries today.