# Discoveries — 2026-09-01

## Investigate
- **ralphex** (https://github.com/umputun/ralphex): Standalone MIT-licensed CLI that takes Markdown implementation plans at the root of a Git repo, breaks them into tasks, spins up fresh Claude Code or Codex sessions per task (avoids context bloat), runs validation/linters/tests after each, supports code-review phases, and handles Git branching/committing. Practical for long-running autonomous coding workflows. Suggested next: Clone repo, test with a sample plan, compare session isolation vs single long-context approaches.
- **Intent.app** (open source multi-agent orchestration layer): Provider-agnostic orchestration for multi-agent systems, works with any coding harness or model (including local). Emphasizes named roles, structured handoffs, shared artifacts/memory. Part of recent Grok Bot ecosystem discussions. Suggested next: Explore the GitHub/setup, map against MCP patterns for tool governance.
- **Unity + LLM tooling bridge** (C# method decoration for LLM tools): Tool that exposes arbitrary C# methods (editor + runtime) as LLM-callable tools via attributes, with native MCP compatibility (Claude Desktop, Cursor, etc.). Turns game engine extensibility into an agentic playground for live telemetry/testing/in-game AI. Suggested next: Review the implementation details or demo for how it handles compiled builds and observability.
- **Dynamic model routing implementations** (e.g. Switchboard Chrome extension agent, OMH "OH MY Hermes" harness, Snowflake Cortex AI Gateway): Real shipped examples of capability + price-based routing to cheapest capable model per step/action, with cost visibility and on-chain elements in some cases. Ties into broader efficiency patterns (quantized local models, structured output wins). Suggested next: Test Switchboard or OMH routing in a small workflow; read the TMLR survey paper on Dynamic Model Routing.

## Interesting
- **Vulcan CLI** (https://github.com/Ellipsis-Labs/vulcan-cli): Open-source CLI + MCP server enabling AI agents (Claude Code, Cursor, Codex) to trade on Phoenix perpetuals (Solana on-chain) with built-in agent support.
- **Grok Bot Teams / PumpGrok / Awesome Grok Bot** resources: Concrete examples of 8-agent crypto desks, role-specialized teams (Helm/Scout/Archive/Forge etc.), shared memory/handoffs, observability dashboards (dsh-context), and "survival swarm" cost-self-funding setups.
- **LLM + persistent notebook for interprocedural static analysis**: Hack where LLM maintains function summaries in a notebook and propagates them across callers for fixed-point analysis (classic compiler technique via chatbot, with hallucination risks noted).
- **Single-prompt → validated articulated 3D asset pipeline**: Frozen LLM builds assembly graph, writes code iteratively under vision critic, carries materials/sim validation — zero 3D training data.
- **Physical AI data loops** (Axis Robotics + KaitoAI attention market): Crowdsourced browser-based robot trajectory teaching at scale (millions of trajectories) turned into public attention/research market.

## Quiet Day
No compelling discoveries today. (N/A — several technical leads above meet the 2-hour investigation bar.)