# Discoveries — 2026-07-25

## Investigate
- **Forge (Rust + Lua CLI)**: New open-source CLI tool that allows adding or altering commands *after* the binary has been built. Positions itself as solving a real extensibility pain point the author had. GitHub: https://github.com/opeolluwa/forge. Worth 2 hours to clone, build, and test dynamic command extension mechanics — reveals design choices around post-build mutability vs traditional static CLIs.
- **Multi-agent graphs & structured workflows**: Posts detail shift from single-agent retry loops to graphs with specialized micro-agents, deterministic state, parallel execution, conditional branching, and human-in-loop. Concrete examples include Claude setups with Architect/Engineer/Reviewer/Optimizer roles, plus adversarial review steps, SOTA research phases, and eval-driven iteration. Links point to detailed personal workflows. Investigate one full example thread for patterns in role specialization and failure handling.
- **Skill tooling architect framework**: Practical decision rules for AI/agent workflows — classify steps as natural-language (judgment/tone), deterministic scripts (calculations/validation), or live tool calls (external data/actions). Includes rules like never script judgment, never leave deterministic steps in prose, add confirmation gates for destructive actions, and define failure behavior explicitly. Ties into meta-tools (find_tool + call_tool) via platforms like Pipedream/Composio for long-tail integrations. High signal for 2-hour experiment building a small pipeline using the classification.
- **Task-level model routing best practices**: Emphasis on owning custom routing logic calibrated to domain-specific evals rather than universal routers. Route at task/sub-task level using trace data; require benchmarking on real workloads, task policies, secret isolation, budget limits, failure traces, and verified fallbacks. Examples from LangChain Labs and critiques of naive "cheapest model" approaches. Worth digging into one detailed thread + related benchmarking discussion for production cost optimization patterns.

## Interesting
- **marimo-pair**: Tool/extension giving AI agents direct access to marimo reactive Python notebooks (git-friendly, run as scripts/apps). Now in Claude Code/Codex CLI marketplaces and VS Code/Cursor extension. Relevant for AI-native notebook workflows.
- **Redpanda Connect visual pipeline composer**: Drag-and-drop interface for building data/streaming pipelines instead of YAML. Mentioned in context of easier cross-tool orchestration.
- **Neon Postgres CLI updates**: New `neon inspect db` (read-only diagnostics) and `neon snapshots` commands, plus MCP server improvements.
- **Unix pipes historical framing**: Revival of how pipes enabled "orgy of one-liners" and compositional thinking in tool bridging.

## Quiet Day
No compelling discoveries today. (All surfaced items fell below the "2-hour investigation yields new knowledge" threshold after filtering for depth vs. announcements.)