# Discoveries — 2026-07-10

## Investigate
- **Hybrid Graph + Vector Memory Systems for Agents**: Combines vector embeddings for semantic similarity with graph structures for relationships (e.g., tickets linked to bugs/releases). Agents observe → retrieve → reason → act → update memory in a loop. Prototype uses Cognee. Foundational upgrade for long-running, non-resetting agent workflows. Link: discussion on X (search for "graph vector memory" recent posts). Next step: Read the thread, prototype a small example with Cognee or similar, test across sessions.
- **LMCache (KV Cache Persistence for Self-Hosted LLMs)**: Open-source tool (vLLM/SGLang compatible) that persists KV cache across GPU/CPU/disk/S3 for repeated contexts (system prompts, RAG chunks, history). Delivers 3-10x faster responses in production; used by Google Cloud/CoreWeave. 8k+ stars. Next step: Install (`pip install lmcache`), benchmark on a local setup with repeated prompts vs. baseline.
- **6-Part Agent Cost Optimization Framework**: Architectural patterns (model routing by complexity/cost/privacy, semantic caching >0.95 similarity, context compression, structured outputs to cut retries, batching, per-step token budgets with middleware). Reports 50-70% cost reductions. Stack: LiteLLM, Redis, instructor, Langfuse. Next step: Map one of your current agent workflows against the 6 parts and measure potential savings.
- **Onboard-CLI**: LLM + AST-based CLI tool for visualizing and onboarding to complex codebases. Recently featured as Show HN. GitHub: https://github.com/animesh-94/Onboard-CLI. Next step: Clone, try on a messy repo, compare to manual exploration or existing tools like `tree` + grep.
- **AirKV (Keyboard-Driven Monitor Switching)**: macOS menu bar app (vibe-coded with Grok/Cursor) that uses Logitech keyboard device switch or Universal Control as source of truth to auto-switch Samsung Smart Monitor input via local WiFi/LAN API. Elegant hardware-software bridge. Next step: Review the implementation details/thread for API usage patterns and how AI tooling accelerated it.

## Interesting
- Cybersecurity AI (CAI) framework: 300+ models supported, built-in recon/exploitation tools, runs without license key. https://github.com/aliasrobotics/cai
- T3MP3ST: Turns local AI coding agent into autonomous zero-day vulnerability hunter.
- Meta Muse Image: Agentic image generation with web search, code execution, self-evaluation/edits during inference (RL-trained). Tool-use at generation time.
- Superhuman Docs "AI Views": Vibe-code interactive apps inside collaborative docs with live two-way data sync to tables (Jira, Salesforce, etc.) and AI running inside the app.
- Shadow Agents + Multipath Reasoning + Verification layers: Reliability patterns for high-stakes/regulated domains (e.g., banking compliance).
- Model routing as first-class architecture (task/cost/privacy-based dispatch, often with cheap classifier + LiteLLM).
- n8n + Claude + Kling/Luma pipeline for automated AI-generated YouTube Shorts from news.
- Atomic Memory / LLM Wiki graph for self-maintaining, linkable, synthesizable knowledge over years.

## Quiet Day
No compelling discoveries today.