# Discoveries — 2026-07-03

## Investigate
- **DOX framework** (https://github.com/agent0ai/dox): Self-documenting open-source framework for autonomous AI coding agents. Uses an `AGENTS.md` file to maintain alignment, reduce context drift, and improve reliability. Recently highlighted in GitHub trending content.  
  **Why worth 2 hours**: Concrete implementation of agent memory/persistence patterns; clone and inspect how AGENTS.md structures agent behavior. Next step: Clone repo, review the markdown spec and example agents, test integration with a simple coding task.

- **Persistent multi-agent VPS orchestration** (thread by @0xsaikarthik): Runs multiple Codex agents across VPS instances, each with goal ownership, custom skills, hooks for follow-ups/sub-agent spawning, and ChatGPT integration for QA/testing. System continues iterating while user is offline, cutting active workload ~50%.  
  **Why worth 2 hours**: Real-world example of scalable, always-on multi-agent systems beyond single prompts. Next step: Extract details on skill/hook implementation; prototype a minimal version using available agent tools or local scripts.

- **Multi-LLM consensus pipeline** (7-phase, 33-step workflow by @SpaceWelder314): Sequences models (GLM + Codex/GPT for audits → Opus review with 5–7 turn consensus loops) for high (~98%) one-shot success on vague tasks. Runs on ~$280/mo subscriptions.  
  **Why worth 2 hours**: Practical pattern for model collaboration and error reduction via structured multi-turn review. Next step: Diagram the phases, identify reusable components (e.g., audit → consensus), experiment with a smaller analog using 2–3 models.

- **Prompt caching stable-prefix technique** (details from @pallavishekhar_ and @hicap_ai): Place unchanging content (system instructions, docs, tools, few-shots) first so variable user input is last; Claude specifics include 1k–4k token minimums, 5-min TTL, per-model caches, and tools like Matcha for breakpoints.  
  **Why worth 2 hours**: Immediate, measurable cost/ latency wins with minimal code change. Next step: Audit existing prompts for prefix stability, implement/test on a real workflow, quantify cache hit rates.

- **Dynamic model routing as collaborative workflow** (insights from AI Engineer conference via @lily_gpupoor and others): Treat routing as ongoing supervision problem (small models for execution/sidekicks, frontier models for judgment/steering); integrate with KV/prefix caching, quantization, speculative decoding.  
  **Why worth 2 hours**: Shifts routing from static cost hacks to production reliability patterns. Next step: Review key concepts mentioned (routing loops, supervision), sketch a simple router prototype or map to existing setups.

## Interesting
- **FlyEnv v4.16.0** (@xpf0000): Lightweight cross-platform dev environment (no Docker) with new built-in MCP Server, AI Coding CLI Manager, and one-click support for Claude Code, Codex, Copilot CLI, etc.
- **n8n packaging playbook** (@Vladic_ETH): Stabilize GitHub n8n workflows (~80% done), hide complexity behind simple forms (Lovable/Cursor), package outputs professionally, and sell as guaranteed-ROI products targeting real business pain points (e.g., lead gen, PDF summarizers).
- **LLM training data preprocessing pipeline** (@successstepss): Detailed multi-stage funnel (ingestion → aggressive filtering ~45%, dedup ~20%, toxicity/quality ~15%, normalization/tokenization) that forms the foundation of model intelligence.
- **Structured output guardrails** (@habuma, @Tech_girlll): Validate/retry on JSON vs. Markdown responses; awareness of "structured-output leaks" as a real prompt injection vector (top OWASP LLM issue).
- **Local LLM scripting optimization** (@tebillusassort): Use frontier models to generate scripts/tools that lightweight/local models can then execute efficiently.

## Quiet Day
No compelling discoveries today.